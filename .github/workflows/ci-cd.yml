name: Deploy AI Caller

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # ---------- Backend ----------
      - name: Deploy Backend
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: 168.231.112.124
          username: root
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          script: |
            set -e
            echo "🚀 Deploying AI Caller Backend..."
            cd /var/www/ai-caller/backend
            git reset --hard
            git pull https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/CodeCafeLab/ai-caller.git main
            npm install --production
            pm2 restart ai-caller-backend
            echo "✅ Backend deployed successfully!"

      # ---------- Frontend ----------
      - name: Deploy Frontend
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: 168.231.112.124
          username: root
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 22
          script: |
            set -e
            echo "🚀 Deploying AI Caller Frontend..."
            cd /var/www/ai-caller
            git reset --hard
            git pull https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/CodeCafeLab/ai-caller.git main
            npm install
            npm run build || { echo "❌ Frontend build failed"; exit 1; }
            pm2 restart ai-caller-frontend
            echo "✅ Frontend deployed successfully!"
